#!/usr/bin/env node
/**
 * Minimal sample app for EvalsHub CI webhook testing.
 * Push changes to this repo to trigger the webhook and see a CI run in EvalsHub.
 */
console.log('EvalsHub CI webhook test app – push this repo to trigger a run.')
